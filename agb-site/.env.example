# Copy to .env.development / .env.staging / .env.production and adjust.
# All runtime vars must be prefixed with VITE_.

VITE_APP_ENV=dev
VITE_AGB_API_URL=http://159.89.146.180:3003
VITE_BASE_URL=/
VITE_OUTPUT_PATH=dist
